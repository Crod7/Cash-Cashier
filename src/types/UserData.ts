// Used to establish what attributes will be added to users throughout the application

interface UserData {
    name?: string;
    budget?: any;
    purchaseHistory?: any;
}

export default UserData;
